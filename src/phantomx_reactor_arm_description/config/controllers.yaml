controller_manager:
  ros__parameters:
    update_rate: 100 # Hz
    # read_rate: 15
    # write_rate: 25 

    # velocity_controller:
    #   type: velocity_controllers/JointGroupVelocityController

    joint_trajectory_controller:
      type: joint_trajectory_controller/JointTrajectoryController

    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

# velocity_controller:
#   ros__parameters:
#     joints:
#       - shoulder_yaw_joint
#       - shoulder_pitch_joint
#       - shoulder_pitch_joint_right
#       - elbow_pitch_joint_right
#       - elbow_pitch_joint
#       - wrist_pitch_joint
#       - wrist_roll_joint
#       - gripper_right_joint

joint_trajectory_controller:
  ros__parameters:
    joints:
      - shoulder_yaw_joint
      - shoulder_pitch_joint
      # - shoulder_pitch_joint_right
      # - elbow_pitch_joint_right
      - elbow_pitch_joint 
      - wrist_pitch_joint
      - wrist_roll_joint
      - gripper_right_joint
    interface_name: position
    command_interfaces:
      - position
      # - velocity
    state_interfaces:
      - position
      # - effort
      # - velocity
  # gains:
  # shoulder_yaw_joint: {p: 100.0, d: 5.0, i: 1.0}
  # shoulder_pitch_joint: {p: 100.0, d: 5.0, i: 1.0}
  # shoulder_pitch_joint_right: {p: 100.0, d: 5.0, i: 1.0}
  # elbow_pitch_joint_right: {p: 100.0, d: 5.0, i: 1.0}
  # elbow_pitch_joint: {p: 100.0, d: 5.0, i: 1.0}
  # wrist_pitch_joint: {p: 100.0, d: 5.0, i: 1.0}
  # wrist_roll_joint: {p: 100.0, d: 5.0, i: 1.0}
  # gripper_right_joint: {p: 100.0, d: 5.0, i: 1.0}